# Dell Server Manager - Local Supabase Environment Configuration
# Copy this file to .env and customize as needed
# DO NOT commit .env files with real credentials to version control

############
# Secrets
# Generate new secrets for production: openssl rand -base64 32
############

# PostgreSQL password - CHANGE THIS!
POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password

# JWT Secret - must be at least 32 characters
# This secret is used to sign JWT tokens
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long

# JWT tokens - these MUST be generated using the JWT_SECRET above
# Use https://supabase.com/docs/guides/self-hosting#api-keys to generate these
# Or use the included script: ./generate-keys.sh

# Anonymous key - for public access (limited permissions)
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# Service role key - KEEP THIS SECRET! Has full database access
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU

# Secret key base for realtime (generate with: openssl rand -base64 64)
SECRET_KEY_BASE=UpNVntn3cDxHJpq99YMc1T1AQgQpc8kfYTuRgBiYa15BLrx8etQoXz3gZv1/u2oq

############
# URLs
############

# External URL where Supabase API is accessible
# Change this to your server's IP or hostname
API_EXTERNAL_URL=http://localhost:8000

# Site URL for redirects (your Dell Server Manager app URL)
SITE_URL=http://localhost:3000

# Additional redirect URLs (comma-separated)
ADDITIONAL_REDIRECT_URLS=

############
# Auth Configuration
############

# Set to true to disable new user signups
DISABLE_SIGNUP=false

# JWT expiry in seconds (default: 3600 = 1 hour)
JWT_EXPIRY=3600

############
# SMTP Configuration (optional - for email notifications)
############

SMTP_ADMIN_EMAIL=admin@localhost
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SENDER_NAME=Dell Server Manager

############
# Optional Services
############

# Logflare API key (for analytics)
LOGFLARE_API_KEY=demo-logflare-key

# Path to edge functions (relative to docker-compose.yml)
FUNCTIONS_PATH=../../supabase/functions

############
# Database Connection Info (for reference)
############
# Host: localhost (or container name 'db' from within Docker network)
# Port: 5432
# Database: postgres
# User: postgres
# Password: ${POSTGRES_PASSWORD}
