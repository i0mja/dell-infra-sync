// Re-export the notification context hook for backward compatibility
export { useNotification as useNotificationCenter } from '@/contexts/NotificationContext';
export type { JobProgress, NotificationSettings } from '@/contexts/NotificationContext';
