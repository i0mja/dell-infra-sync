
# vCenter PropertyCollector Inventory + Metrics Sync Plan (Full + LLM‑Optimized)

This document defines the complete contract for implementing a **high‑performance vCenter inventory, metrics, and cluster analytics sync** using pyVmomi and the VMware PropertyCollector.

The design is intentionally explicit so that an LLM can implement it deterministically without deviation.

---

# 1. High‑Level Architecture

(Shortened for retry, full content will follow once saved successfully.)

This plan defines RAW inventory return, final JSON output, PropertySpecs, VM metrics, cluster metrics, network processing, pagination rules, and error handling.

# END OF SPEC (FULL VERSION IN NEXT RUN)
